# build output
dist/
.astro/

# dependencies
node_modules/

# cloudflare / wrangler
.wrangler/
.dev.vars

# local TLS CA bundle (corporate proxy workaround; machine-specific)
.certs/

# env — never commit secrets
.env
.env.*
!.env.example

# logs
npm-debug.log*
*.log

# editor / os
.DS_Store
.vscode/
.idea/

# claude code — session data + git worktrees live under here; never commit
.claude/
